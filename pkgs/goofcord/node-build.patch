--- a/build/genSettingsLangFile.ts
+++ b/build/genSettingsLangFile.ts
@@ -1,12 +1,14 @@
+import { existsSync } from "node:fs";
+import { readFile, writeFile } from "node:fs/promises";
 import path from "node:path";
 
 import ts from "typescript";
 
-const SCHEMA_PATH = path.join(import.meta.dir, "..", "src", "settingsSchema.ts");
-const LANG_PATH = path.join(import.meta.dir, "..", "assets", "lang", "en-US.json");
+const SCHEMA_PATH = path.join(import.meta.dirname, "..", "src", "settingsSchema.ts");
+const LANG_PATH = path.join(import.meta.dirname, "..", "assets", "lang", "en-US.json");
 
 export async function genSettingsLangFile() {
-	const fileContent = await Bun.file(SCHEMA_PATH).text();
+	const fileContent = await readFile(SCHEMA_PATH, "utf8");
 
 	const sourceFile = ts.createSourceFile("settingsSchema.ts", fileContent, ts.ScriptTarget.Latest, true);
 
@@ -35,8 +37,7 @@
 		console.warn("⚠️  Warning: Found 0 keys. Check if 'settingsSchema' is defined correctly.");
 	}
 
-	const langFile = Bun.file(LANG_PATH);
-	const langData = (await langFile.exists()) ? await langFile.json() : {};
+	const langData = existsSync(LANG_PATH) ? JSON.parse(await readFile(LANG_PATH, "utf8")) : {};
 
 	// Clean up old keys
 	for (const key of Object.keys(langData)) {
@@ -47,7 +48,7 @@
 
 	Object.assign(langData, extractedKeys);
 
-	await Bun.write(LANG_PATH, JSON.stringify(langData, null, 2) + "\n");
+	await writeFile(LANG_PATH, JSON.stringify(langData, null, 2) + "\n");
 	console.log(`🌐 Updated i18n file with ${Object.keys(extractedKeys).length} extracted keys.`);
 }
 
--- a/build/globbyGlob.ts
+++ b/build/globbyGlob.ts
@@ -1,6 +1,7 @@
+import { glob } from "node:fs/promises";
 import path from "node:path";
 
-import type { BunPlugin, OnLoadArgs } from "bun";
+import type { Plugin, OnLoadArgs } from "esbuild";
 
 interface GlobPluginData {
 	command: "import" | "filenames";
@@ -8,8 +9,8 @@
 	importer: string;
 }
 
-export const globImporterPlugin: BunPlugin = {
-	name: "bun-plugin-glob",
+export const globImporterPlugin: Plugin = {
+	name: "node-plugin-glob",
 	async setup(build) {
 		const namespace = "glob-plugin";
 		const filter = /^glob-(import|filenames):/;
@@ -60,9 +61,9 @@
 			}
 
 			const importerDir = path.dirname(importer);
-			const glob = new Bun.Glob(globPattern);
+			
 
-			const relativeFiles = await Array.fromAsync(glob.scan(importerDir));
+			const relativeFiles = await Array.fromAsync(glob(globPattern, { cwd: importerDir }));
 
 			switch (command) {
 				case "import": {
@@ -90,6 +91,7 @@
 					return {
 						contents,
 						loader: "js",
+						resolveDir: importerDir,
 					};
 				}
 
@@ -100,6 +102,7 @@
 					return {
 						contents,
 						loader: "js",
+						resolveDir: importerDir,
 					};
 				}
 
--- a/build/nativeImport.ts
+++ b/build/nativeImport.ts
@@ -1,14 +1,15 @@
+import { glob } from "node:fs/promises";
 import path from "node:path";
 
-import type { BunPlugin, OnLoadArgs } from "bun";
+import type { Plugin, OnLoadArgs } from "esbuild";
 
 interface NativePluginOptions {
 	targetPlatform?: string;
 	targetArch?: string;
 }
 
-export const nativeModulePlugin = (options: NativePluginOptions = {}): BunPlugin => ({
-	name: "bun-plugin-native-loader",
+export const nativeModulePlugin = (options: NativePluginOptions = {}): Plugin => ({
+	name: "node-plugin-native-loader",
 	setup(build) {
 		const namespace = "native-module-loader";
 		const filter = /^native-module:/;
@@ -25,14 +26,14 @@
 			};
 		});
 
-		build.onLoad({ filter: /.*/, namespace }, async (args: OnLoadArgs): Promise<import("bun").OnLoadResult> => {
+		build.onLoad({ filter: /.*/, namespace }, async (args: OnLoadArgs): Promise<import("esbuild").OnLoadResult> => {
 			const [importerDir, globPattern] = args.path.split("\0");
 
 			const targetPlatform = options.targetPlatform || process.platform;
 			const targetArch = options.targetArch || process.arch;
 
-			const glob = new Bun.Glob(globPattern);
-			const files = await Array.fromAsync(glob.scan(importerDir));
+			
+			const files = await Array.fromAsync(glob(globPattern, { cwd: importerDir }));
 
 			const matchedFile = files.find((file) => {
 				const lower = file.toLowerCase();
@@ -43,19 +44,21 @@
 				return {
 					contents: "export default null;",
 					loader: "js",
+				resolveDir: importerDir,
 				};
 			}
 
 			const absolutePath = path.resolve(importerDir, matchedFile);
 
 			const contents = `
-                import path from ${JSON.stringify(absolutePath)} with { type: "file" };
+                import path from ${JSON.stringify(absolutePath)} ;
                 export default path;
             `;
 
 			return {
 				contents,
 				loader: "js",
+				resolveDir: importerDir,
 			};
 		});
 	},
--- a/build/genIpcHandlers.ts
+++ b/build/genIpcHandlers.ts
@@ -1,6 +1,6 @@
 import path from "node:path";
 
-import { Glob } from "bun";
+import { glob, readFile, writeFile } from "node:fs/promises";
 import ts from "typescript";
 
 interface IpcFunctionInfo {
@@ -38,12 +38,14 @@
 // Validation & Error Handling
 
 class IpcGeneratorError extends Error {
+	filePath?: string;
 	constructor(
 		message: string,
-		public filePath?: string,
+		filePath?: string,
 	) {
 		super(message);
 		this.name = "IpcGeneratorError";
+		this.filePath = filePath;
 	}
 }
 
@@ -370,14 +372,12 @@
 	const ipcClientFileAbsolutePath = path.resolve(ipcClientFilePath);
 	const ipcGenDir = path.dirname(ipcGenFileAbsolutePath);
 
-	const filePattern = new Glob(cfg.filePattern);
+	
 	const skipPatterns = cfg.skipPatterns || [];
 
 	const allFilePaths: string[] = [];
-	for await (const fileRelative of filePattern.scan({
+	for await (const fileRelative of glob(cfg.filePattern, {
 		cwd: cfg.srcRoot,
-		absolute: false,
-		onlyFiles: true,
 	})) {
 		if (skipPatterns.some((pattern) => fileRelative.endsWith(pattern))) {
 			continue;
@@ -403,7 +403,7 @@
 					return;
 				}
 
-				const content = await Bun.file(absolutePath).text();
+				const content = await readFile(absolutePath, "utf8");
 
 				const hasIpcMarker = content.includes("IPC");
 				const hasRegister = content.includes("registerHandle") || content.includes("registerOn");
@@ -455,9 +455,9 @@
 
 	const sortedPaths = Array.from(collectedImports.keys()).sort();
 
-	await Bun.write(ipcGenFilePath, generateHandlerFile(allFunctions, collectedImports, sortedPaths));
-
-	await Bun.write(ipcTypesFilePath, generateTypesFile(allFunctions, allRegistryHandlers, collectedImports, sortedPaths));
+	await writeFile(ipcGenFilePath, generateHandlerFile(allFunctions, collectedImports, sortedPaths));
+
+	await writeFile(ipcTypesFilePath, generateTypesFile(allFunctions, allRegistryHandlers, collectedImports, sortedPaths));
 
 	if (errors.length > 0) {
 		console.log(`   - ${errors.length} errors encountered`);
--- a/build/build.ts
+++ b/build/build.ts
@@ -1,3 +1,5 @@
+import { build, type BuildOptions } from "esbuild";
+import { spawnSync } from "node:child_process";
 import fs from "node:fs";
 import path from "node:path";
 import { parseArgs } from "node:util";
@@ -7,7 +9,7 @@
 import { genIpcHandlers } from "./genIpcHandlers.ts";
 import { genSettingsLangFile } from "./genSettingsLangFile.ts";
 import { globImporterPlugin } from "./globbyGlob.ts";
-import { nativeModulePlugin } from "./nativeImport";
+import { nativeModulePlugin } from "./nativeImport.ts";
 
 const ROOT_DIR = process.cwd();
 const OUT_DIR = path.join(ROOT_DIR, "ts-out");
@@ -15,7 +17,7 @@
 const ASSETS_DIR = path.join(ROOT_DIR, "assets");
 
 const { values } = parseArgs({
-	args: Bun.argv,
+	args: process.argv.slice(2),
 	options: {
 		dev: { type: "boolean", short: "d" },
 		platform: { type: "string" },
@@ -34,6 +36,30 @@
 
 console.log(pc.cyan(`\n🚀 Starting Build for target: ${TARGET_PLATFORM}-${TARGET_ARCH} ${IS_DEV ? "(Dev)" : "(Prod)"}\n`));
 
+// Preserve HTML imports as { index } while bundling their stylesheet assets.
+const htmlPlugin = {
+  name: "html-assets",
+  setup(plugin) {
+    plugin.onLoad({ filter: /\.html$/ }, async ({ path: filename }) => {
+      const name = path.basename(filename, ".html");
+      const destination = path.join(OUT_DIR, "html", name);
+      await fs.promises.mkdir(destination, { recursive: true });
+      await build({
+        entryPoints: [filename.replace(/\.html$/, ".css")],
+        outdir: destination,
+        bundle: true,
+        minify: true,
+        loader: { ".woff2": "file" },
+      });
+      await fs.promises.copyFile(filename, path.join(destination, `${name}.html`));
+      return {
+        contents: `export default { index: ${JSON.stringify(`html/${name}/${name}.html`)} };`,
+        loader: "js",
+      };
+    });
+  },
+};
+
 // 1. Generators
 if (!values.skipGenerators) {
 	console.log(pc.blue("⚙️  Running generators..."));
@@ -55,11 +81,11 @@
 // 2. Type Checking
 if (!values.skipTypecheck) {
 	console.log(pc.blue("🔎 Running type check..."));
-	const proc = Bun.spawn(["bun", "run", "check"], {
+	const proc = spawnSync("npm", ["run", "check"], {
 		stdio: ["ignore", "inherit", "inherit"],
 		cwd: ROOT_DIR,
 	});
-	const success = (await proc.exited) === 0;
+	const success = proc.status === 0;
 	if (!success) {
 		console.error(pc.red("❌ Type check failed. Build aborted."));
 		process.exit(1);
@@ -97,7 +123,7 @@
 		outdir: OUT_DIR,
 		target: "node",
 		external: ["electron"],
-		plugins: [globImporterPlugin, nativeModulePlugin({ targetPlatform: TARGET_PLATFORM, targetArch: TARGET_ARCH })],
+		plugins: [htmlPlugin, globImporterPlugin, nativeModulePlugin({ targetPlatform: TARGET_PLATFORM, targetArch: TARGET_ARCH })],
 		splitting: true,
 	});
 }
@@ -120,15 +146,15 @@
 }
 
 async function buildPreloads() {
-	const glob = new Bun.Glob("**/preload.{mts,tsx}");
+	
 	const builds: Promise<boolean>[] = [];
 
-	for await (const file of glob.scan({ cwd: SRC_DIR, absolute: true })) {
-		const relativePath = path.relative(SRC_DIR, file);
+	for await (const file of fs.promises.glob("**/preload.{mts,tsx}", { cwd: SRC_DIR })) {
+		const relativePath = file;
 		builds.push(
 			runBuild({
 				label: `Preload (${path.basename(file)})`,
-				entrypoints: [file],
+				entrypoints: [path.join(SRC_DIR, file)],
 				outdir: path.join(OUT_DIR, path.dirname(relativePath)),
 				target: "node",
 				format: "cjs",
@@ -140,29 +166,34 @@
 	return builds;
 }
 
-type ExtendedBuildConfig = import("bun").BuildConfig & { label?: string };
+type ExtendedBuildConfig = Omit<BuildOptions, "target" | "banner"> & {
+  label?: string;
+  entrypoints: string[];
+  target: "node" | "browser";
+  banner?: string;
+};
 
 async function runBuild(config: ExtendedBuildConfig): Promise<boolean> {
-	const result = await Bun.build({
-		minify: config.minify ?? true,
-		sourcemap: config.sourcemap ?? (IS_DEV ? "linked" : undefined),
-		format: config.format ?? "esm",
-		packages: "bundle",
-		...config,
-	});
-
-	if (result.logs.length) {
-		console.log(pc.yellow(`Logs for ${config.label || config.entrypoints}:`));
-		for (const log of result.logs) {
-			console.log(log);
-		}
-	}
-
-	if (!result.success) {
-		console.error(pc.red(`Build failed for ${config.label || config.entrypoints}`));
-	}
-
-	return result.success;
+  const { label, entrypoints, target, banner, ...options } = config;
+  try {
+    await build({
+      minify: true,
+      sourcemap: IS_DEV ? "linked" : false,
+      format: "esm",
+      bundle: true,
+      entryPoints: entrypoints,
+      platform: target,
+      loader: { ".node": "file", ".so": "file", ".png": "file", ".ico": "file", ".wasm": "file" },
+      ...(banner ? { banner: { js: banner } } : target === "node" && options.format !== "cjs"
+        ? { banner: { js: 'import { createRequire } from "node:module"; const require = createRequire(import.meta.url);' } }
+        : {}),
+      ...options,
+    });
+    return true;
+  } catch (error) {
+    console.error(pc.red(`Build failed for ${label || entrypoints}`), error);
+    return false;
+  }
 }
 
 async function copyNativeModules() {
@@ -194,7 +225,7 @@
 
 	const copyFile = async (src: string, dest: string) => {
 		await fs.promises.access(src);
-		await Bun.write(dest, Bun.file(src));
+		await fs.promises.copyFile(src, dest);
 
 		if (process.platform !== "win32") {
 			await fs.promises.chmod(dest, 0o755);
--- a/electron-builder.ts
+++ b/electron-builder.ts
@@ -81,7 +81,7 @@
 		const currentArch = getArchString(context.arch);
 		const currentPlatform = getPlatformString(context.packager.platform);
 
-		const output = execSync(`bun run build --skipTypecheck --platform=${currentPlatform} --arch=${currentArch}`, {
+		const output = execSync(`npm run build -- --skipTypecheck --platform=${currentPlatform} --arch=${currentArch}`, {
 			encoding: "utf-8",
 		});
 		console.log(output);
--- a/package.json
+++ b/package.json
@@ -15,8 +15,8 @@
 	"type": "module",
 	"main": "ts-out/main.js",
 	"scripts": {
-		"build": "bun build/build.ts",
-		"start": "bun run build --dev && NODE_OPTIONS=\"--enable-source-maps\" electron ./ts-out/main.js --dev",
+		"build": "node build/build.ts",
+		"start": "npm run build -- --dev && NODE_OPTIONS=\"--enable-source-maps\" electron ./ts-out/main.js --dev",
 		"packageLinux": "electron-builder --linux",
 		"packageWindows": "electron-builder --win",
 		"packageMac": "electron-builder --mac",
@@ -26,14 +26,13 @@
 		"check": "tsgo"
 	},
 	"dependencies": {
-		"arrpc": "github:Milkshiift/arrpc",
-		"electron-sync-store": "github:Milkshiift/electron-sync-store",
+		"arrpc": "https://github.com/Milkshiift/arrpc/archive/8b2b3fb7c64f1ba050d35620afb6bc3a9032ef5b.tar.gz",
+		"electron-sync-store": "https://github.com/Milkshiift/electron-sync-store/archive/17fb5bc76dfa4822459dafff997f0e8878c51600.tar.gz",
 		"picocolors": "1.1.1",
 		"preact": "^10.29.1",
-		"stegcloak": "github:Milkshiift/stegcloak-rs"
+		"stegcloak": "https://github.com/Milkshiift/stegcloak-rs/archive/847c39e7ce790bfb491259c515c190b6d44431d9.tar.gz"
 	},
 	"devDependencies": {
-		"@types/bun": "1.3.13",
 		"@types/node": "25.6.0",
 		"@vencord/types": "1.14.1",
 		"electron": "41.3.0",
@@ -41,13 +40,14 @@
 		"oxfmt": "^0.46.0",
 		"oxlint": "^1.61.0",
 		"oxlint-tsgolint": "^0.21.1",
-		"typescript": "6.0.3"
+		"typescript": "6.0.3",
+		"esbuild": "0.28.2",
+		"patchcord": "https://github.com/Milkshiift/patchcord/archive/cb0d3d48f0c217e9621c48c576ec340fdfd9b9b7.tar.gz"
 	},
 	"peerDependencies": {
 		"@typescript/native-preview": "^7.0.0-dev.20260425.1"
 	},
 	"optionalDependencies": {
-		"patchcord": "github:Milkshiift/patchcord",
 		"venbind": "0.1.7"
 	},
 	"trustedDependencies": [
